info:
  name: login-doctor
  type: http
  seq: 1

http:
  method: POST
  url: "{{doctor}}/login"
  body:
    type: json
    data: |-
      {
          "email" : "dhruv@gmail.com",
          "password" : "1234"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
