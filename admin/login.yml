info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{admin}}/login"
  body:
    type: json
    data: |-
      {
          "email": "dhruv@gmail.com",
          "password": "admin1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
