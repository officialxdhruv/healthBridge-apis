info:
  name: login-user
  type: http
  seq: 2

http:
  method: POST
  url: "{{user}}/login"
  body:
    type: json
    data: |-
      {
          "email" : "dhruv@gmail.com",
          "password": "12345678"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
