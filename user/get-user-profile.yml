info:
  name: get-user-profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{user}}/get-profile"
  body:
    type: json
    data: |-
      {
        "email": "dhruv@gmail.com",
        "password": "12345678"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
